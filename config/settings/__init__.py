"""
Django settings package.

Select module via DJANGO_SETTINGS_MODULE:

- ``config.settings.development`` — local development (SQLite by default).
- ``config.settings.production`` — Ubuntu / PostgreSQL / reverse proxy (Caddy).
"""
